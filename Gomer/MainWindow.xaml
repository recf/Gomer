<Window x:Class="Gomer.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:gomer="clr-namespace:Gomer"
        mc:Ignorable="d"
        Title="Gomer" 
        ResizeMode="CanResize"
        SizeToContent="WidthAndHeight"
        MinHeight="500"
        MinWidth="800"
        DataContext="{Binding Source={StaticResource Locator}, Path=Main}"
        
        d:DesignWidth="525"
        d:DesignHeight="350">
    <DockPanel>
        <TabControl>
            <TabItem Header="Pile" Content="{Binding PileGames}"/>
            <TabItem Header="Played" Content="{Binding PlayedGames}"/>
            <TabItem Header="Wishlist" Content="{Binding WishlistGames}"/>
            <TabItem Header="Ignored" Content="{Binding IgnoredGames}"/>
        </TabControl>
    </DockPanel>
</Window>
