<UserControl x:Class="Gomer.PileGames.PileGameDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:pileGames="clr-namespace:Gomer.PileGames"
             mc:Ignorable="d" 
             
             d:DataContext="{d:DesignInstance Type=pileGames:PileGameDetailViewModel}"
             d:DesignHeight="300" 
             d:DesignWidth="300">
    <StackPanel>
        <Label Content="Name"/>
        <TextBox Text="{Binding Name}"/>
        
        <Label Content="Platform"/>
        <ComboBox ItemsSource="{Binding Platforms}" SelectedItem="{Binding Platform}" DisplayMemberPath="Name"/>
        
        <WrapPanel>
            <Button Content="Ok" Command="{Binding OkCommand}"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}"/>
        </WrapPanel>
    </StackPanel>
</UserControl>
