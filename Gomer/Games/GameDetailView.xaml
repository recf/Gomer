<UserControl x:Class="Gomer.Games.GameDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:games="clr-namespace:Gomer.Games"
             mc:Ignorable="d" 
             
             d:DataContext="{d:DesignInstance Type=games:GameDetailViewModel}"
             d:DesignHeight="300" 
             d:DesignWidth="300">
    <StackPanel>
        <GroupBox Header="Details">
            <StackPanel>

                <Label Content="List"/>
                <ComboBox SelectedItem="{Binding Game.List}"
                          ItemsSource="{Binding Lists}"
                          DisplayMemberPath="Name"/>

                <Label Content="Name"/>
                <TextBox Text="{Binding Game.Name}"/>

                <Label Content="Platform"/>
                <TextBox Text="{Binding Game.Platform}" />

                <Label Content="Estimated Hours"/>
                <TextBox Text="{Binding Game.EstimatedHours}"/>

                <Label Content="Played Hours"/>
                <TextBox Text="{Binding Game.PlayedHours}"/>

                <Label Content="Added On"/>
                <DatePicker SelectedDate="{Binding Game.AddedOn}"/>

                <Label Content="Started On"/>
                <DatePicker SelectedDate="{Binding Game.StartedOn}"/>

                <Label Content="Finished On"/>
                <DatePicker SelectedDate="{Binding Game.FinishedOn}"/>

                <StackPanel Margin="5">
                    <Button Content="Ok" Command="{Binding UpdateCommand}"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}"/>
                </StackPanel>

                <Expander Header="Delete">
                    <Button Content="Confirm Delete" Command="{Binding RemoveCommand}" />
                </Expander>
            </StackPanel>
        </GroupBox>
    </StackPanel>
</UserControl>
