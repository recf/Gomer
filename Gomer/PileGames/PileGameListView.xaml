<UserControl x:Class="Gomer.PileGames.PileGameListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:pileGames="clr-namespace:Gomer.PileGames"
             mc:Ignorable="d" 
             
             d:DataContext="{d:DesignInstance Type=pileGames:PileGameListViewModel}"
             d:DesignHeight="300"
             d:DesignWidth="300">
    <Grid>
        <DataGrid ItemsSource="{Binding Games}" 
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Platform" Binding="{Binding Platform}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
